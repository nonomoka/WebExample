@using WebExample.Models.Replay;
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    List<ReplayMatchList> MatchList = ViewBag.MatchList;
}
<div class="container-fluid">
    <div class="page-header">
        <h3>查詢賽事列表</h3>
    </div>
    @*<div class="row">
        <div class="col-xs-2" style="padding-right:2px;text-align:right;">
            <span class="label label-default" style="font-size:115%;line-height:2;">輸入日期</span>
        </div>
        <div class="col-xs-10" style="padding-left:2px;">
            <div class="input-group">
                <div class='input-group date'>
                    <input type="Date" class="form-control" id="InputDate" />
                </div>
            </div>
        </div>
    </div>
    <br />
    <div class="row">
        <div class="col-xs-2" style="padding-right:2px;text-align:right;">
            <button type="button" class="btn btn-primary" id="btnMatchesSelect">
                <span class="glyphicon glyphicon-search"></span>
                查詢賽事資料
            </button>
        </div>
        <div class="col-xs-10" style="padding-left:2px;">
            <button type="button" class="btn" id="btnMatchesClear">清除</button>
        </div>
    </div>*@
    <br />
    <div class="row">
        <div class="col-xs-12">
            <table class="table table-striped" id="MatchesList">
                <thead>
                    <tr>
                        <th>MatchID</th>
                        <th>聯賽</th>
                        <th>主隊</th>
                        <th>客隊</th>
                        <th>比分</th>
                        <th>開賽時間</th>
                    </tr>
                </thead>
                <tbody>
                    @for (int i = 0; i < MatchList.Count(); i++)
                    {
                        <tr>
                            <td>@MatchList[i].MatchID</td>
                            <td>@MatchList[i].TournamentZH</td>
                            <td>@MatchList[i].Team1ZH</td>
                            <td>@MatchList[i].Team2ZH</td>
                            <td>@MatchList[i].MatchScore</td>
                            <td>@MatchList[i].MatchDate</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    <br />
    <div class="page-header">
        <h3>重播賽事(自選)</h3>
    </div>
    <br />
    <div class="row">
        <div class="col-xs-2" style="padding-right:2px;text-align:right;">
            <span class="label label-default" style="font-size:115%;line-height:2;">執行時間(分)</span>
        </div>
        <div class="col-xs-10" style="padding-left:2px;">
            <div class="input-group">
                <input type="text" class="form-control" maxlength="3" id="CustomMatchesTime" />
            </div>
        </div>
    </div>
    <br />
    <div class="row">
        <div class="col-xs-2" style="padding-right:2px;text-align:right;">
            <span class="label label-default" style="font-size:115%;line-height:2;width:500px;">輸入MatchID</span>
        </div>
        <div class="col-xs-10" style="padding-left:2px;">
            <div class="input-group">
                <input type="text" class="form-control" placeholder="範例:1300121,1400121" id="CustomInputMatchesID" />
            </div>
        </div>
    </div>
    <br />
    <div class="row">
        <div class="col-xs-2" style="padding-right:2px;text-align:right;">
            <button type="button" class="btn btn-primary" id="btnCustomMatches">執行</button>
        </div>
        <div class="col-xs-10" style="padding-left:2px;">
        </div>
    </div>
    <br />
    <div class="page-header">
        <h3>重播賽事(隨機)</h3>
    </div>
    <br />
    <div class="row">
        <div class="col-xs-2" style="padding-right:2px;text-align:right;">
            <span class="label label-default" style="font-size:115%;line-height:2;">執行時間(分)</span>
        </div>
        <div class="col-xs-10" style="padding-left:2px;">
            <div class="input-group">
                <input type="text" class="form-control" maxlength="3" id="RandomMatchesTime" />
            </div>
        </div>
    </div>
    <br />
    <div class="row">
        <div class="col-xs-2" style="padding-right:2px;text-align:right;">
            <span class="label label-default" style="font-size:115%;line-height:2;">輸入重播場數</span>
        </div>
        <div class="col-xs-10" style="padding-left:2px;">
            <div class="input-group">
                <input type="text" class="form-control" id="RandomMatchesCnt" />
            </div>
        </div>
    </div>
    <br />
    <div class="row">
        <div class="col-xs-2" style="padding-right:2px;text-align:right;">
            <button type="button" class="btn btn-primary" id="btnRandomMatches">執行</button>
        </div>
        <div class="col-xs-10" style="padding-left:2px;">
        </div>
    </div>
    <br />
    <div class="page-header">
        <h3>賽事重播列表</h3>
        <button type="button" class="btn btn-info" id="btnRefreshPlayList">
            <span class="glyphicon glyphicon-refresh"></span>
            刷新
        </button>
    </div>
    <br />
    <div class="row">
        <div class="col-xs-12">
            <table class="table table-striped" id="MatchesPlayList">
                <thead>
                    <tr>
                        <th>MatchID</th>
                        <th>聯賽</th>
                        <th>主隊</th>
                        <th>客隊</th>
                        @*<th>強制關閉</th>*@
                    </tr>
                </thead>
                <tbody>
                    @*<tr>
                            <td>13670010</td>
                            <td>巴西</td>
                            <td>英格蘭</td>
                            <td>05:00</td>
                            <td>
                                <div>
                                    <button type="button" class="btn btn-danger">關閉</button>
                                </div>
                            </td>
                        </tr>
                        <tr>
                            <td>13670011</td>
                            <td>法國</td>
                            <td>德國</td>
                            <td>29:00</td>
                            <td>
                                <div>
                                    <button type="button" class="btn btn-danger">關閉</button>
                                </div>
                            </td>
                        </tr>
                        <tr>
                            <td>13670081</td>
                            <td>台灣</td>
                            <td>哥斯大黎加</td>
                            <td>60:00</td>
                            <td>
                                <div>
                                    <button type="button" class="btn btn-danger">關閉</button>
                                </div>
                            </td>
                        </tr>*@
                </tbody>
            </table>
        </div>
    </div>
    <br />
</div>
<script>
    //init
    //if (g_month < 10)
    //    g_month = padLeft(g_month, 2);
    //if (g_day < 10)
    //    g_day = padLeft(g_day, 2);

    //$('#InputDate').val(g_year + '-' + g_month + '-' + g_day);

    
    //查詢賽事列表
    @*$('#btnMatchesSelect').on('click', function () {
        $(".loading-mask").show();
        var data = {
            matchDate: $('#InputDate').val()
        }
        var ajaxRequest = $.ajax({
            type: "POST",
            url: "@Url.Action("MatchListGet", new { controller = "Home"})",
            dataType: "json",
            data: data,
            success: function (data) {
                if (data.Success) {
                    var cnt = data.ResultData.length;
                    if (cnt > 0) {
                        var tbody = '';
                        for (var i = 0; i < cnt; i++) {
                            tbody += '<tr>';
                            tbody += '<td>' + data.ResultData[i].MatchID + '</td>';
                            tbody += '<td>' + data.ResultData[i].TournamentZH + '</td>';
                            tbody += '<td>' + data.ResultData[i].Team1ZH + '</td>';
                            tbody += '<td>' + data.ResultData[i].Team2ZH + '</td>';
                            tbody += '<td>' + data.ResultData[i].MatchScore + '</td>';
                            tbody += '<td>' + data.ResultData[i].MatchDate + '</td>';
                            tbody += '</tr>';
                        }
                        $("#MatchesList").find('tbody').html(tbody);
                        $("#MatchesList").show();
                    }
                    else {
                        alert('查無資料');
                        $("#MatchesList").find('tbody').html('');
                        $("#MatchesList").hide();
                    }
                }
                else {
                    alert(data.ResultData);
                }
            },
            error: function (xhr, ajaxOptions, thrownError) {

            },
            complete: function () {
                $(".loading-mask").hide();
            }
        })
    })

    //清除賽事列表
    $('#btnMatchesClear').on('click', function () {
        $("#MatchesList").find('tbody').html('');
        $("#MatchesList").hide();
    })*@

    //執行重播(Custom)
    $('#btnCustomMatches').on('click', function () {
          $(".loading-mask").show();
        var data = {
            customTime: $('#CustomMatchesTime').val(),
            matchIdList: $('#CustomInputMatchesID').val()
        }
        var ajaxRequest = $.ajax({
            type: "POST",
            url: "@Url.Action("CustomRun", new { controller = "Home"})",
            dataType: "json",
            data: data,
            success: function (data) {
                if (data.Success) {
                    var cnt = data.ResultData.length;
                    if (cnt > 0) {
                        var tbody = '';
                        for (var i = 0; i < cnt; i++) {
                            tbody += '<tr>';
                            tbody += '<td>' + data.ResultData[i].MatchID + '</td>';
                            tbody += '<td>' + data.ResultData[i].TournamentZH + '</td>';
                            tbody += '<td>' + data.ResultData[i].Team1ZH + '</td>';
                            tbody += '<td>' + data.ResultData[i].Team2ZH + '</td>';
                            tbody += '</tr>';
                        }
                        $("#MatchesPlayList").find('tbody').html(tbody);
                        $("#MatchesPlayList").show();
                    }
                    else {
                        $("#MatchesPlayList").find('tbody').html('');
                        $("#MatchesPlayList").hide();
                    }
                }
                else {
                    $("#MatchesPlayList").find('tbody').html('');
                    $("#MatchesPlayList").hide();
                }
            },
            error: function (xhr, ajaxOptions, thrownError) {

            },
            complete: function () {
                $(".loading-mask").hide();
            }
        })
    })

    //執行重播(random)
    $('#btnRandomMatches').on('click', function () {
          $(".loading-mask").show();
        var data = {
            randomTime: $('#RandomMatchesTime').val(),
            randomCnt: $('#RandomMatchesCnt').val()
        }
        var ajaxRequest = $.ajax({
            type: "POST",
            url: "@Url.Action("RadomRun", new { controller = "Home"})",
            dataType: "json",
            data: data,
            success: function (data) {
                if (data.Success) {
                    var cnt = data.ResultData.length;
                    if (cnt > 0) {
                        var tbody = '';
                        for (var i = 0; i < cnt; i++) {
                            tbody += '<tr>';
                            tbody += '<td>' + data.ResultData[i].MatchID + '</td>';
                            tbody += '<td>' + data.ResultData[i].TournamentZH + '</td>';
                            tbody += '<td>' + data.ResultData[i].Team1ZH + '</td>';
                            tbody += '<td>' + data.ResultData[i].Team2ZH + '</td>';
                            tbody += '</tr>';
                        }
                        $("#MatchesPlayList").find('tbody').html(tbody);
                        $("#MatchesPlayList").show();
                    }
                    else {
                        $("#MatchesPlayList").find('tbody').html('');
                        $("#MatchesPlayList").hide();
                    }
                }
                else {
                    $("#MatchesPlayList").find('tbody').html('');
                    $("#MatchesPlayList").hide();
                }
            },
            error: function (xhr, ajaxOptions, thrownError) {

            },
            complete: function () {
                $(".loading-mask").hide();
            }
        })
    })

     //刷新
    $('#btnRefreshPlayList').on('click', function () {
        $(".loading-mask").show();
        getEList();
    })


    function getEList()
    {
        var data = {}
         var ajaxRequest = $.ajax({
            type: "POST",
            url: "@Url.Action("RefreshExecuteList", new { controller = "Home"})",
            dataType: "json",
            data: data,
            success: function (data) {
                if (data.Success) {
                    var cnt = data.ResultData.length;
                    if (cnt > 0) {
                        var tbody = '';
                        for (var i = 0; i < cnt; i++) {
                            tbody += '<tr>';
                            tbody += '<td>' + data.ResultData[i].MatchID + '</td>';
                            tbody += '<td>' + data.ResultData[i].TournamentZH + '</td>';
                            tbody += '<td>' + data.ResultData[i].Team1ZH + '</td>';
                            tbody += '<td>' + data.ResultData[i].Team2ZH + '</td>';
                            //tbody += '<td><div>' + '<button type="button" class="btn btn-danger">關閉</button>' + '</div></td>';
                            tbody += '</tr>';
                        }
                        $("#MatchesPlayList").find('tbody').html(tbody);
                        $("#MatchesPlayList").show();
                    }
                    else {
                        $("#MatchesPlayList").find('tbody').html('');
                        $("#MatchesPlayList").show();
                    }
                }
                else {
                    $("#MatchesPlayList").find('tbody').html('');
                    $("#MatchesPlayList").show();
                }
            },
            error: function (xhr, ajaxOptions, thrownError) {

            },
            complete: function () {
                $(".loading-mask").hide();
            }
        })
    }

</script>
